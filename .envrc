#!/bin/bash
# Ansible vault password file configuration
export ANSIBLE_VAULT_PASSWORD_FILE="/Users/terrac/.ansible_vault_pass"
echo "🔐 Ansible vault password file set: $ANSIBLE_VAULT_PASSWORD_FILE"
