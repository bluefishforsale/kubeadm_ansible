name: Validate Infrastructure Changes

on:
  pull_request:
    branches: [ main, master ]
    paths-ignore:
      - 'README.md'
      - 'docs/**'
      - '*.md'
  workflow_dispatch:

jobs:
  ansible-lint:
    name: Ansible Syntax & Lint
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install Ansible and dependencies
      run: |
        pip install ansible-core ansible-lint yamllint
        pip install kubernetes PyYAML
        
    - name: Create ansible.cfg if missing
      run: |
        if [ ! -f ansible.cfg ]; then
          cat > ansible.cfg << EOF
        [defaults]
        inventory = inventories/production/hosts.ini
        roles_path = roles
        retry_files_enabled = False
        host_key_checking = False
        
        [privilege_escalation]
        become = True
        become_method = sudo
        EOF
        fi
        
    - name: Validate inventory syntax
      run: |
        ansible-inventory --list -i inventories/production/hosts.ini > /dev/null
        
    - name: Ansible syntax check - main playbook
      env:
        ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
      run: |
        # Create temporary vault password file
        echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/.vault_pass
        chmod 600 /tmp/.vault_pass
        
        ansible-playbook site.yml --syntax-check --vault-password-file=/tmp/.vault_pass
        
        # Clean up
        rm -f /tmp/.vault_pass
        
    - name: Ansible syntax check - monitoring playbook
      if: hashFiles('k8s-health-monitoring/playbooks/deploy-monitoring.yml') != ''
      env:
        ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
      run: |
        # Create temporary vault password file
        echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/.vault_pass
        chmod 600 /tmp/.vault_pass
        
        ansible-playbook k8s-health-monitoring/playbooks/deploy-monitoring.yml --syntax-check --vault-password-file=/tmp/.vault_pass
        
        # Clean up
        rm -f /tmp/.vault_pass
        
    - name: Ansible-lint validation
      run: |
        ansible-lint --version
        ansible-lint site.yml || true  # Don't fail on warnings initially
        
    - name: YAML lint validation
      run: |
        # Lint all YAML files
        find . -name "*.yml" -o -name "*.yaml" | grep -v .git | xargs yamllint -d relaxed

  kubernetes-manifests:
    name: Kubernetes Manifest Validation
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'v1.32.0'
        
    - name: Validate Kubernetes manifests
      run: |
        # Find all K8s YAML manifests and validate them
        find . -path "./.git" -prune -o -name "*.yaml" -print0 | while IFS= read -r -d '' file; do
          if grep -q "apiVersion.*v1\|apps/v1\|rbac" "$file" 2>/dev/null; then
            echo "Validating K8s manifest: $file"
            kubectl --dry-run=client --validate=true apply -f "$file"
          fi
        done

  security-scan:
    name: Security & Best Practices
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Scan for secrets
      uses: trufflesecurity/trufflehog@main
      with:
        path: ./
        base: main
        head: HEAD
        
    - name: Check for hardcoded IPs/passwords
      run: |
        # Basic security checks
        echo "Checking for hardcoded secrets..."
        
        # Check for potential hardcoded passwords (but allow known config)
        if grep -r "password\s*=" . --exclude-dir=.git | grep -v "ansible_vault\|vault_password\|ANSIBLE_VAULT" | head -5; then
          echo "⚠️ Potential hardcoded passwords found (review above)"
        fi
        
        # Check for private keys (except vault encrypted)
        if find . -name "*.pem" -o -name "*_rsa" | head -5; then
          echo "⚠️ Private key files found (review if needed)"
        fi
        
        echo "✅ Security scan complete"

  dry-run-test:
    name: Dry Run Test
    runs-on: ubuntu-latest
    needs: [ansible-lint, kubernetes-manifests]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python & Ansible
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install Ansible
      run: |
        pip install ansible-core kubernetes PyYAML
        
    - name: Create mock inventory for dry run
      run: |
        mkdir -p inventories/test
        cat > inventories/test/hosts.ini << EOF
        [proxmox]
        localhost ansible_connection=local
        
        [k8s:children]
        k8s_controller
        k8s_worker
        
        [k8s_controller]
        localhost ansible_connection=local
        
        [k8s_worker]
        localhost ansible_connection=local
        EOF
        
    - name: Dry run - main playbook
      env:
        ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
      run: |
        # Create temporary vault password file
        echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/.vault_pass
        chmod 600 /tmp/.vault_pass
        
        ansible-playbook site.yml -i inventories/test/hosts.ini --check --diff --vault-password-file=/tmp/.vault_pass || true
        
        # Clean up
        rm -f /tmp/.vault_pass
        
    - name: Dry run - monitoring playbook
      if: hashFiles('k8s-health-monitoring/playbooks/deploy-monitoring.yml') != ''
      env:
        ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
      run: |
        # Create temporary vault password file
        echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/.vault_pass
        chmod 600 /tmp/.vault_pass
        
        ansible-playbook k8s-health-monitoring/playbooks/deploy-monitoring.yml -i inventories/test/hosts.ini --check --diff --vault-password-file=/tmp/.vault_pass || true
        
        # Clean up
        rm -f /tmp/.vault_pass
        
    - name: Generate deployment summary
      run: |
        echo "## 🚀 Deployment Validation Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "✅ Ansible syntax validation passed" >> $GITHUB_STEP_SUMMARY
        echo "✅ Kubernetes manifests validated" >> $GITHUB_STEP_SUMMARY
        echo "✅ Security scan completed" >> $GITHUB_STEP_SUMMARY
        echo "✅ Dry run test executed" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "🎯 Ready for deployment after merge!" >> $GITHUB_STEP_SUMMARY